import { Metadata } from 'next'

export const metadata: Metadata = {
  title: "登录 | Muzzle's blog",
  description: "Muzzle's blog Login Page.",
}

export default function Login() {
  return <div>login page</div>
}
